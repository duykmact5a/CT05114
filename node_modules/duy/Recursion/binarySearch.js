function binarySearch(list,element){
    var half=Math.floor(list.length/2);
    if (element==list[half]) return half;
    if (element>list[half])  return half + list.slice(half,list.length).binarySearch(element); 
    else return list.slice(0, half).binarySearch(elememt);
}