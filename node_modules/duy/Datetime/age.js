function age(theDay) {
    var diffDate = Date.now() - theDay.getTime();
    var output = new Date(diffDate);
    return Math.abs(output.getUTCFullYear() - 1970);
}
console.log(age(new Date(1962, 1, 1)));
