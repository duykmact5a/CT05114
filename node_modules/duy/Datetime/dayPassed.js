function dayPassed(theDay) {
  var current = new Date(theDay.getTime());
  var previous = new Date(theDay.getFullYear(), 0, 1);
  return Math.ceil((current - previous + 1) / 86400000);
}
console.log(dayPassed(new Date(2015, 0, 15))); 