function count(element,list){
    var result=0;
    for (var i=0;i<list.length;i++){
        if (list[i]== element) result++;
    }
    return result;
}
function countChar(char,string){
    stringList=string.split("");
    return count(char,stringList);
}