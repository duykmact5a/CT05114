function shuffle(list) {
    var len = list.length, temp, index;
    while (len > 0) {
        index = Math.floor(Math.random() * len);
        len--;
        temp = list[len];
        list[len] = list[index];
        list[index] = temp;
    }
    return list;
}